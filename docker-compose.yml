services:
  vite-app:
    build:
      context: ./client
      dockerfile: Dockerfile
    ports:
      - "3000:80"

  express-app:
    build:
      context: ./server
      dockerfile: Dockerfile
    env_file:
      - server/.env
    ports:
      - "8000:8000"
      - "10000-10100:10000-10100/udp"
